import styled from 'styled-components';
// import { provider } from 'styles';

const Company: any = {};

Company.Root = styled.div`
  padding: 8rem 0;
  min-height: 100vh;
`;

export default Company;
